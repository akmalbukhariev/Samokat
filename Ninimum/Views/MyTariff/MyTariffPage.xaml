<?xml version="1.0" encoding="utf-8" ?>
<page:BasePage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:language="clr-namespace:Ninimum.Resources.Languages"
    xmlns:page="clr-namespace:Ninimum.Views"
    xmlns:components="clr-namespace:Ninimum.Components"
    x:Class="Ninimum.Views.MyTariff.MyTariffPage"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False"
    x:Name="ThisPage"
    BackgroundColor="White">

    <page:BasePage.Resources>
        <ResourceDictionary>
            <Style x:Key="TariffLabelStyle" TargetType="Label">
                <Setter Property="FontFamily" Value="{StaticResource FontSFRegular}" />
                <Setter Property="FontSize" Value="17" />
                <Setter Property="TextColor" Value="#96979B" />
            </Style>

            <Style x:Key="TariffValueStyle" TargetType="Label">
                <Setter Property="FontFamily" Value="{StaticResource FontSFMedium}" />
                <Setter Property="FontSize" Value="17" />
                <Setter Property="TextColor" Value="#22292F" />
            </Style>
        </ResourceDictionary>
    </page:BasePage.Resources>

    <Grid RowDefinitions="Auto,*">

        <components:PageHeaderView
            Grid.Row="0"
            Title="{x:Static language:AppResource.MyTariff}" />

        <ScrollView Grid.Row="1" VerticalScrollBarVisibility="Never">
            <VerticalStackLayout Padding="20,24,20,24" Spacing="24">

                <!-- Current active tariff -->
                <Border
                    IsVisible="{Binding HasActiveSubscription}"
                    Padding="14"
                    BackgroundColor="White"
                    Stroke="#F78A08"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 10">

                    <VerticalStackLayout Spacing="14">

                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{x:Static language:AppResource.Status}" Style="{StaticResource TariffLabelStyle}" />
                            <Label Text="{x:Static language:AppResource.Active}" TextColor="#16A34A" FontFamily="{StaticResource FontSFMedium}" FontSize="17" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{x:Static language:AppResource.TariffPlan}" Style="{StaticResource TariffLabelStyle}" />
                            <Label Text="{Binding CurrentTariffName}" Style="{StaticResource TariffValueStyle}" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{x:Static language:AppResource.SubscriptionDate}" Style="{StaticResource TariffLabelStyle}" />
                            <Label Text="{Binding CurrentStartDate}" Style="{StaticResource TariffValueStyle}" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{x:Static language:AppResource.UntilTariffEnds}" Style="{StaticResource TariffLabelStyle}" />
                            <Label Text="{Binding CurrentRemainingText}" Style="{StaticResource TariffValueStyle}" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{x:Static language:AppResource.Price}" Style="{StaticResource TariffLabelStyle}" />
                            <Label Text="{Binding CurrentPrice}" Style="{StaticResource TariffValueStyle}" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Spacing="10">
                            <Label Text="{x:Static language:AppResource.TariffEndDate}" Style="{StaticResource TariffLabelStyle}" />
                            <Label Text="{Binding CurrentEndDate}" Style="{StaticResource TariffValueStyle}" />
                        </HorizontalStackLayout>

                        <!-- Active users can open TariffsPage and switch to another plan. -->
                        <Button
                            Text="{x:Static language:AppResource.ChangeTariff}"
                            HeightRequest="54"
                            Margin="0,6,0,0"
                            CornerRadius="12"
                            BackgroundColor="#FF4A3F"
                            TextColor="White"
                            FontSize="17"
                            FontFamily="{StaticResource FontSFBold}"
                            Command="{Binding JoinTariffCommand}" />

                        <Button
                            Text="{x:Static language:AppResource.CancelTariff}"
                            HeightRequest="54"
                            Margin="0,0,0,0"
                            CornerRadius="12"
                            BackgroundColor="White"
                            BorderColor="#FF4A3F"
                            BorderWidth="1"
                            TextColor="#FF4A3F"
                            FontSize="17"
                            FontFamily="{StaticResource FontSFBold}"
                            Command="{Binding CancelTariffCommand}" />

                    </VerticalStackLayout>
                </Border>

                <!-- No active tariff -->
                <Border
                    IsVisible="{Binding HasActiveSubscription, Converter={StaticResource InverseBoolConverter}}"
                    Padding="18"
                    BackgroundColor="#F7F8FB"
                    Stroke="#DADADA"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 10">

                    <VerticalStackLayout Spacing="16">
                        <Label
                            Text="{x:Static language:AppResource.YouDoNotHaveAnActiveTariffPlease}"
                            TextColor="#22292F"
                            FontSize="17"
                            FontFamily="{StaticResource FontSFRegular}"
                            LineBreakMode="WordWrap" />

                        <Button
                            Text="{x:Static language:AppResource.SubscribeToATariff_1c0091}"
                            HeightRequest="54"
                            CornerRadius="12"
                            BackgroundColor="#FF4A3F"
                            TextColor="White"
                            FontSize="17"
                            FontFamily="{StaticResource FontSFBold}"
                            Command="{Binding JoinTariffCommand}" />
                    </VerticalStackLayout>
                </Border>

                <!-- History header -->
                <VerticalStackLayout
                    IsVisible="{Binding HasHistory}"
                    Spacing="10">

                    <HorizontalStackLayout
                        HorizontalOptions="Center"
                        Spacing="14">

                        <HorizontalStackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ToggleHistoryCommand}" />
                        </HorizontalStackLayout.GestureRecognizers>

                        <Label
                            Text="{x:Static language:AppResource.TariffHistory}"
                            FontFamily="{StaticResource FontSFMedium}"
                            FontSize="15"
                            TextColor="#96979B"
                            VerticalOptions="Center" />

                        <Image
                            Source="{Binding HistoryArrowIcon}"
                            WidthRequest="24"
                            HeightRequest="24"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>

                    <CollectionView
                        ItemsSource="{Binding TariffHistory}"
                        IsVisible="{Binding IsHistoryVisible}"
                        SelectionMode="None">

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Padding="10"
                                    Margin="0,0,0,10"
                                    BackgroundColor="#F7F8FB"
                                    Stroke="#DADADA"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 10">

                                    <VerticalStackLayout Spacing="14">

                                        <HorizontalStackLayout Spacing="10">
                                            <Label Text="{x:Static language:AppResource.TariffPlan}" Style="{StaticResource TariffLabelStyle}" />
                                            <Label Text="{Binding Plan}" Style="{StaticResource TariffValueStyle}" />
                                        </HorizontalStackLayout>

                                        <HorizontalStackLayout Spacing="10">
                                            <Label Text="{x:Static language:AppResource.SubscriptionDate}" Style="{StaticResource TariffLabelStyle}" />
                                            <Label Text="{Binding StartDate}" Style="{StaticResource TariffValueStyle}" />
                                        </HorizontalStackLayout>

                                        <HorizontalStackLayout
                                            Spacing="10"
                                            IsVisible="{Binding IsActive, Converter={StaticResource InverseBoolConverter}}">
                                            <Label Text="{x:Static language:AppResource.SubscriptionEndDate}" Style="{StaticResource TariffLabelStyle}" />
                                            <Label Text="{Binding EndDate}" Style="{StaticResource TariffValueStyle}" />
                                        </HorizontalStackLayout>

                                        <HorizontalStackLayout Spacing="10">
                                            <Label Text="{x:Static language:AppResource.Price}" Style="{StaticResource TariffLabelStyle}" />
                                            <Label Text="{Binding Price}" Style="{StaticResource TariffValueStyle}" />
                                        </HorizontalStackLayout>

                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>

                    </CollectionView>

                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

        <components:LoadingView
            x:Name="loading"
            Grid.RowSpan="2"
            ZIndex="100" />

    </Grid>
</page:BasePage>
