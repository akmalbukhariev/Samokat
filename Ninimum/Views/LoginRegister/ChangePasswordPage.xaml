<page:BasePage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:language="clr-namespace:Ninimum.Resources.Languages"
    xmlns:page="clr-namespace:Ninimum.Views"
    xmlns:components="clr-namespace:Ninimum.Components"
    x:Class="Ninimum.Views.LoginRegister.ChangePasswordPage"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False"
    x:Name="ThisPage"
    BackgroundColor="White">

    <Grid>
        <ScrollView>
            <VerticalStackLayout
                Padding="20,20,30,50"
                Spacing="24">

                <components:PageHeaderView
                    Title="{x:Static language:AppResource.NewPassword}" />

                <Label
                    Text="{x:Static language:AppResource.CreateANewPasswordForSecurityPleaseEnter}"
                    HorizontalTextAlignment="Center"
                    TextColor="#96979B"
                    FontSize="17"
                    FontFamily="{StaticResource FontSFRegular}" />

                <components:InputView
                    x:Name="txtPassword"
                    Title="{x:Static language:AppResource.NewPassword}"
                    LeftIcon="ic_lock.png"
                    IsPassword="True" />

                <components:InputView
                    x:Name="txtConfirmPassword"
                    Title="{x:Static language:AppResource.RepeatPassword}"
                    LeftIcon="ic_lock.png"
                    IsPassword="True" />

                <components:CustomActionButton
                    ButtonText="{x:Static language:AppResource.Save}"
                    ButtonBackgroundColor="#FD473C"
                    ButtonTextColor="White"
                    ButtonImageSource="right.png"
                    TapCommand="{Binding Source={x:Reference ThisPage}, Path=SaveCommand}" />

            </VerticalStackLayout>
        </ScrollView>

        <components:LoadingView
            x:Name="loading"
            ShowLoading="{Binding IsLoading}" />
    </Grid>

</page:BasePage>
