<?xml version="1.0" encoding="utf-8" ?>
<page:BasePage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:language="clr-namespace:Ninimum.Resources.Languages"
    xmlns:page="clr-namespace:Ninimum.Views"
    xmlns:components="clr-namespace:Ninimum.Components"
    x:Class="Ninimum.Views.ChangePhoneNumber.ChangePhoneNumberPage"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False"
    x:Name="ThisPage"
    BackgroundColor="White">

    <Grid RowDefinitions="Auto,*,Auto">

        <components:PageHeaderView
            Grid.Row="0"
            Title="{x:Static language:AppResource.ChangePhoneNumber}" />

        <ScrollView Grid.Row="1" VerticalScrollBarVisibility="Never">
            <VerticalStackLayout Padding="20,24,20,24" Spacing="24">

                <VerticalStackLayout Spacing="4">
                    <Label
                        Text="{x:Static language:AppResource.YourCurrentPhoneNumber}"
                        FontFamily="{StaticResource FontSFRegular}"
                        FontSize="17"
                        TextColor="#96979B" />

                    <Label
                        Text="{Binding CurrentPhoneNumber}"
                        FontFamily="{StaticResource FontSFMedium}"
                        FontSize="17"
                        TextColor="#7CB518" />
                </VerticalStackLayout>

                <Label
                    Text="{x:Static language:AppResource.EnterYourNewPhoneNumberTheNewNumber}"
                    FontFamily="{StaticResource FontSFRegular}"
                    FontSize="17"
                    TextColor="#22292F"
                    LineBreakMode="WordWrap" />

                <Border
                    Padding="10"
                    BackgroundColor="#FFF2F3"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 8">
                    <Label
                        Text="{x:Static language:AppResource.AttentionAfterVerificationYouWillNeedToSign}"
                        FontFamily="{StaticResource FontSFRegular}"
                        FontSize="13"
                        TextColor="#EA1111" />
                </Border>

                <components:InputView
                    Title="{x:Static language:AppResource.NewPhoneNumber}"
                    Placeholder="90 123 45 67"
                    LeftIcon="ic_phone.png"
                    Keyboard="Telephone"
                    ClearButtonVisibility="WhileEditing"
                    Text="{Binding PhoneNumber}" />

                <Grid ColumnDefinitions="24,*" ColumnSpacing="12">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ToggleAgreementCommand}" />
                    </Grid.GestureRecognizers>

                    <Image
                        Source="{Binding AgreementIcon}"
                        WidthRequest="24"
                        HeightRequest="24"
                        VerticalOptions="Center" />

                    <Label
                        Grid.Column="1"
                        Text="{x:Static language:AppResource.IConfirmThatTheEnteredPhoneNumberBelongs}"
                        FontFamily="{StaticResource FontSFRegular}"
                        FontSize="16"
                        TextColor="#22292F"
                        LineBreakMode="WordWrap" />
                </Grid>

            </VerticalStackLayout>
        </ScrollView>

        <Button
            Grid.Row="2"
            Text="{x:Static language:AppResource.Continue}"
            HeightRequest="56"
            CornerRadius="8"
            Margin="15,0,15,24"
            BackgroundColor="{Binding SubmitButtonColor}"
            TextColor="White"
            FontFamily="{StaticResource FontSFMedium}"
            FontSize="18"
            IsEnabled="{Binding CanSubmit}"
            Command="{Binding SubmitCommand}" />

        <components:SmsCodePopup
            x:Name="popupSms"
            Grid.RowSpan="3"
            ConfirmCommand="{Binding Source={x:Reference ThisPage}, Path=BindingContext.ConfirmSmsCodeCommand}"
            ResendCommand="{Binding Source={x:Reference ThisPage}, Path=BindingContext.ResendSmsCommand}"
            IsVisible="False"
            ZIndex="100" />

        <components:LoadingView
            Grid.RowSpan="3"
            ShowLoading="{Binding IsLoading}"
            ZIndex="110" />

    </Grid>
</page:BasePage>
