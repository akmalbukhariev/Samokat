<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Ninimum.Components.CustomLikedView" 
             xmlns:local="clr-namespace:Ninimum.Components"
             x:DataType="local:CustomLikedView">

     <Border x:Name="borderContainer"
            Opacity="0.4"
            StrokeThickness="1"
            Stroke="Black"
            BackgroundColor="Black"
            Padding="0"
            WidthRequest="60"
            HeightRequest="60">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="5"/>
        </Border.StrokeShape>
        <Grid>
            <Grid.Resources>
                <Style x:Key="imHeartStyle" TargetType="Image">
                  <Setter Property="Aspect" Value="AspectFit"/>
                  <Setter Property="WidthRequest" Value="40"/>
                  <Setter Property="HeightRequest" Value="40"/>
                  <Setter Property="VerticalOptions" Value="Center"/>
                  <Setter Property="HorizontalOptions" Value="Center"/>
                </Style>
            </Grid.Resources>
            <Image x:Name="imLiked" 
                   Source="liked.png" 
                   IsVisible="False"
                   Style="{StaticResource imHeartStyle}"/>
            <Image x:Name="imUnLiked" 
                   Source="unliked.png" 
                   IsVisible="True"
                   Style="{StaticResource imHeartStyle}"/>
        </Grid>
    </Border>
</ContentView>
